# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
dependency-graph.dot
dependency-graph.svg

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# IDE / agent session logs (not project files)
terminals/
log.txt

# Local-only notes (not for GitHub) — lives under docs/private/
docs/private/
docs/archive/
docs/
playtest/
kimi/
daimon-share/
/tools/


./deepcode/
./agents/
./tmp/


# Local agent/design specs (not for GitHub)
docs/superpowers/

# Deep Code CLI local config + skills (not project files)
.deepcode/
.tmp/
.agents/
BUG_REPORTS/
skills/

# Local-only session transcript (not for GitHub)
Wilderfolk.txt

# Local config with secrets — never commit
.env

# Git worktrees
.worktrees

# Windows shortcut (accidental local file)
*.lnk

# Playwright / playtest artifacts
playtest/
test-results/
playwright-report/

.bug-hunter/
