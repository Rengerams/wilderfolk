# Wilderfolk Bug & Logic Flaw Audit
**Date:** 2026-07-08 | **Files:** 50 | **Total Issues:** 87

## 🔴 High Severity

| # | File | Type | Description |
|---|------|------|-------------|
| 1 | worldGen.ts | Bug | `pregnant: undefined` is set in base entity, but later `if (opts?.pregnant)` sets it to true. However, `pregnantById` is set in the pregnancy block but `pregnant` itself is set to `true` without checking if entity is female. Male immigrants in `createImmigrantSettler` can be passed `pregnant: true` via the wife's options, but the husband entity is not marked pregnant - this is correct. However, the wife's `pregnantById` uses `fatherId` which could be undefined if `opts.fatherId` is not set. |
| 2 | groupEvents.ts | Bug | Checks `e.faction !== 'visitor' && e.faction !== 'rival' && e.faction !== 'trade_caravan'`, but `trade_caravan` is not a faction value in the Entity type - it's a job type. Caravan entities have `faction: 'trade_caravan'` according to tradeCaravans.ts, but the type definition may not include it. This could exclude trade caravan merchants from being considered 'player humans'. |
| 3 | groupEvents.ts | Bug | Population is recalculated by iterating `rival.entityIds` and checking alive, but `rival.population` is also incremented when a new family joins (`rival.population++`). This double-counts: the loop sets population, then later `rival.population++` adds to it. Actually, looking closer: the loop sets `rival.population = pop`, then later if a new family joins, `rival.population++` is done. But on the next tick, the loop will recalculate from entityIds, so the increment is lost. Wait, no - the loop runs first, then the increment happens after. So the increment persists until next tick. But if the loop runs and finds 5 alive, sets pop=5, then adds 1 making pop=6, but entityIds now has 6. Next tick loop finds 6 alive, sets pop=6. So it's actually correct, just redundant. |
| 4 | groupEvents.ts | Bug | Massive function with nested switch cases. In 'tribute' case, 'pay' choice: checks `state.resources.food >= 30` but if it fails, doesn't return early - it falls through without setting `resolved=true`. However, the outer logic only removes the event if `resolved` is true. So if the player clicks 'pay' but lacks food, the event remains but no feedback is given beyond the float text. Actually, looking at the code, if food < 30, it pushes a float but doesn't set resolved=true, so the event stays. This is correct behavior (player can try again). |
| 5 | humanChat.ts | Bug | `const entityA = self.id === session.entityAId ? self : (partner ?? self);` - If `self.id !== session.entityAId` and `partner` is null, `entityA` becomes `self`. But `entityB` is set to `self.id === session.entityBId ? self : partner`. If session is not solo and partner is null, this could cause entityB to be null while entityA is self, which is inconsistent. The clearDialogueSession call might not clear the right entities. |
| 6 | lifeSimulation.ts | Bug | Function iterates `state.entities` and `newEntities` to build a list of living humans. But `newEntities` may contain newly created humans that are also in `state.entities` (if `pushNewEntity` adds to both). Actually, `pushNewEntity` pushes to `ctx.newEntities` but NOT to `state.entities`. So new entities this tick are only in `ctx.newEntities`. But `allLivingHumans` is called in `tickHumans` after `pushNewEntity` for childbirth. The child is in `newEntities` but not `state.entities`, so it won't be found by `allLivingHumans` until next tick. This means `rebuildChildrenIds(allLivingHumans(state, newEntities))` might miss the newborn in some edge cases. |
| 7 | lifeSimulation.ts | Bug | `forEachEntityInRadius` with `mobileGrid` or `playerHumans` fallback. If `mobileGrid` exists, it uses `forEachInRadius` with a callback that modifies `paramour`. But `forEachEntityInRadius` in spatialGrid.ts iterates buckets and calls the callback. However, the callback in `tryDailyAffairEncounter` only considers candidates with `entity.id < paramour.id` AFTER finding them. But `paramour` is reassigned inside `considerParamour`, which is called for every human in radius. The `bestDistSq` starts at 120*120. But if the first candidate is at dist 100, `paramour` is set. Then if a second candidate is at dist 80, it replaces. But the `entity.id >= paramour.id` check at the end means if the closest paramour has a higher ID, no affair is started. This is intentional (only one partner initiates), but the logic is fragile. |
| 8 | lifeSimulation.ts | Bug | `if (paramour.partnerId != null && isAtMaritalHome(paramour, entityById, buildingById)) { if (isSpouseAtSharedHome(paramour, entityById, buildingById, intimateDist)) return false; if (hourOfDay != null && shouldBeAtHome(hourOfDay)) return false; return false; }` - The final `return false` means that if paramour is married and at their marital home, the tryst is ALWAYS invalid, even if the spouse is not there and it's not home time. This seems overly restrictive. |
| 9 | lifeSimulation.ts | Bug | `if (entity.gender !== 'female' \|\| entity.pregnant \|\| entity.reproductionCooldown > 0) return false;` - But later it checks `entity.relationshipStatus === 'married' && entity.partnerId`. If the partner is dead (`getLivingEntity` returns undefined), it still proceeds to check `together` which uses `dist` from the dead partner's last position. If partner is dead, `partner` is undefined, so `together` is false. But the affair conception branch checks `hasAffairPartner` which uses `entity.affairPartnerId`. If the affair partner is dead, `getLivingEntity` returns undefined, but `hasAffairPartner` still returns true because it only checks `entity.affairPartnerId != null`. Then `tryst` check uses `isValidAffairConceptionSite` which checks `getLivingEntity(entity.affairPartnerId, ctx.entityById)` - wait, no, `isValidAffairConceptionSite` calls `isValidAffairTrystSite` which doesn't check if the lover is alive. |
| 10 | lifeSimulation.ts | Bug | After divorce, it reassigns residences using `pickResidenceForHuman`. But `pickResidenceForHuman` might pick the same residence for both, causing them to still share a home. Also, `syncResidenceOccupants` is called but the old home's occupants were already filtered. However, if `residences.length === 0`, the code doesn't handle unhoused divorcees. |
| 11 | lifeSimulation.ts | Bug | `if (entity.pregnancyProgress >= PREGNANCY_TICKS)` - When birth happens, `entity.pregnant = false` and `entity.pregnancyProgress = 0`. But `entity.pregnantById = undefined` is set. If the baby is a bastard, the biological father is notified. But if the husband is dead, `husband?.alive` check prevents flash. However, `if (husband?.alive && !isBastard)` adds the child to husband's childrenIds. But if husband is alive and it's a bastard (biological father is not husband), the husband does NOT get the child in his childrenIds. This is correct for bastards. |
| 12 | lifeSimulation.ts | Bug | `closestPrey.alive = false;` - When a human hunts prey, the prey is killed immediately. But `syncEntityGrids(ctx, closestPrey)` is called after death. However, if the prey was already hunted by another entity this tick, it might be dead already. The check `closestPrey && closestDist < config.size + closestPrey.size` doesn't verify `closestPrey.alive` again after the distance check. Actually, it does check `closestPrey` exists, but if it died between the find and the kill (single-threaded, so impossible), it's fine. But the `huntTargetId` is set on the entity, and if the prey is killed by another hunter first, this entity will still try to move toward the dead prey. |
| 13 | lifeSimulation.ts | Bug | `const sameTypeCount = wildlifeTypePopulation(wildlifePopulation, entity.type, entity.id);` - This excludes the parent's own new spawns from the count. But `entity.id` is passed, and `newSpawnedByParent.get(parentEntityId)` returns the count. However, if the parent has already spawned once this tick, the second spawn check will see `sameTypeCount` excluding the first spawn, so it might spawn again. But `entity.reproductionCooldown` is set to `config.reproductionCooldown` after spawning, preventing same-tick multiple births. |
| 14 | renderer.ts | Bug | `const buckets = updateRenderSoABuckets(...)` returns `shimBySlot` but `_cachedHumans` is built from `entitiesFromSoASlots(buckets.humanSlots, buckets.shimBySlot)`. If `buckets.shimBySlot` doesn't contain all human slots (e.g., due to a bug in bucket update), some humans won't render. Also, `syncDrawCacheTick` resets cache on tick change, but if the same tick is rendered multiple times (e.g., multiple frames per tick), the cache is rebuilt unnecessarily. |
| 15 | saveLoad.ts | Bug | `world.entities = (worldData.entities \|\| []).map(...)` - When migrating legacy moon howlers, `migrateLegacyMoonHowler` is called for each entity. But if a werewolf is cured (no longer cursed), it might not be converted back to human properly. Also, `ensureEntitySkills` is called but skills are not persisted in save data (not in WORLD_STATE_SAVE_KEYS), so skills are lost on load. |
| 16 | spatialGrid.ts | Bug | `insert` checks `if (!entity.alive) return;` but `update` is called with `entity` that might have just died. If `update` is called after death, the entity is removed by `remove(entity)` then `insert` returns early. This is correct. But if `update` is called when entity is alive and hasn't moved, it still does `removeById` + `insert`, which is O(1) but unnecessary. |
| 17 | tradeCaravans.ts | Bug | `const carrier = createEntity(...)` creates a human with `faction: 'trade_caravan'`. But `isPlayerHuman` in groupEvents.ts excludes `faction === 'trade_caravan'`, so the merchant won't be counted as a player human. This affects population counts, housing, and potentially other systems that filter by `isPlayerHuman`. |
| 18 | worldEvents.ts | Bug | `if (isProductionTick(state.tick, EVENT_INTERVAL.disaster) && state.year > 3 && Math.random() < 0.15)` - Disasters only check on production ticks. But `EVENT_INTERVAL.disaster` might be every 60 ticks. If the random check fails, no disaster happens. But the `state.disasters` array is processed every tick to update progress. However, active disasters don't continue to deal damage - they just exist for `duration` ticks. The initial damage is applied once when created. This is correct. |
| 19 | moonHowler.ts | Bug | `human.type = EntityType.Werewolf;` changes the entity type. But `isPlayerHuman` checks `e.type === EntityType.Human`, so transformed werewolves are no longer considered player humans. This means they won't be processed by `tickHumans` in the human section - they'll be processed as wildlife. But they still have human properties (job, residence, etc.). In `lifeSimulation.ts`, `tickHumans` iterates `allHumans = byType[EntityType.Human]`, so werewolves are skipped. But `tickWildlife` iterates `WILDLIFE_TICK_TYPES` which includes `EntityType.Werewolf`. So they get processed as wildlife. However, they might still have `homeBuildingId` and other human-specific properties that wildlife logic doesn't handle. |
| 20 | villageLeadership.ts | Bug | `if (ceremony.phase === 'reveal') { try { ... } finally { state.electionCeremony = null; } }` - The ceremony is cleared even if `runVillageElection` throws an exception. But if an exception occurs, the election result is lost and no leader is appointed. The `try/finally` ensures the ceremony doesn't get stuck, but the error is not logged or handled. |
| 21 | stripTopology.ts | Bug | `const existing = findStripBuildingAt(state, junctionPt.x, junctionPt.y, JUNCTION_PROXIMITY * 0.75);` - This finds ANY strip building near the junction. If there's a road at the same location, it might be found instead of a wall. But `findStripBuildingAt` checks `WALL_STRIP_TYPES` and `ROAD_STRIP_TYPES`, so it could find a road and treat it as a wall. The function doesn't filter by family. |
| 22 | lifeSimulation.ts | Bug | `if (isSpouseNearby(entity, entityById, 22)) return false;` - The spouse check uses a hardcoded 22-unit radius. But `isSpouseNearby` in the affair encounter uses 120 units for finding paramours, and 95 for tryst sites. The 22-unit check here is much tighter, meaning affairs can only start if the spouse is very far away. This might be intentional but seems inconsistent with the 55-unit check in `isSpouseNearby` elsewhere. |
| 23 | lifeSimulation.ts | Bug | When a werewolf kills a human, `killHuman` is called. But `killHuman` might trigger death particles, notifications, and building occupancy cleanup. However, the werewolf itself is a cursed human. If the werewolf kills its own spouse, the spouse's `partnerId` is cleared by `killHuman`, but the werewolf's `partnerId` might still point to the dead spouse. This could cause issues in relationship checks. |

## 🟠 Medium Severity

| # | File | Type | Description |
|---|------|------|-------------|
| 1 | worldGen.ts | Logic Flaw | Attempt loop uses `count * 16` but doesn't break early when map is mostly impassable. Could infinite-loop in edge cases if `count` is large and passable tiles are scarce. |
| 2 | worldGen.ts | Bug | If `maxRadius <= 0`, falls back to `spawnWildlifeAtRandomPassable` but ignores the `minDist` parameter entirely. Also, `effMin = Math.min(minDist, effMax)` could be 0, causing spawn at center. |
| 3 | worldGen.ts | Bug | `birthYear: isHuman ? 0 : -1` - For non-human entities, birthYear is set to -1, but in lifeSimulation.ts `entity.age++` is called on new calendar day. This means wildlife age starts at 0 and increments, but birthYear=-1 doesn't align with age tracking. |
| 4 | worldGen.ts | Logic Flaw | Husband's `relationshipStatus` is set to 'married' but wife's is set to 'married' after creation. The husband's `partnerId` is set to wife.id, but wife's `partnerId` is set via opts. This is redundant but consistent. However, if `Math.random() < 0.12` fails, a single newcomer is created with `relationshipStatus: 'single'` but no `partnerId` check. |
| 5 | groupEvents.ts | Bug | `newCalendarDay` is computed as `state.tick > 0 && state.tick % TICKS_PER_DAY === 0`. If TICKS_PER_DAY is, say, 24, then tick 24 is day 1. But `daysLeft--` happens on tick 24, 48, etc. However, visitors arriving on tick 0 with daysLeft=12 would have daysLeft decremented on tick 24 (first day end), so they stay 12 full days plus the arrival day? The timing of 'daysLeft' decrement vs arrival is ambiguous. |
| 6 | groupEvents.ts | Logic Flaw | `const deer = allAlive.find((e) => e.alive && e.type === EntityType.Deer)` - only finds ONE deer and may kill it repeatedly if the same deer is found each day. The deer is killed (`deer.alive = false`) but on the next tick, a new deer might be found. However, if no deer exist, nothing happens. But the poach chance applies per day, not per deer, so a single hunter group can only kill one deer per day max. |
| 7 | groupEvents.ts | Logic Flaw | Tense rivals can reduce reputation every action day (`state.villageReputation = Math.max(0, state.villageReputation - 2)`), but there's no cap on how often this happens. With multiple rivals, reputation can drain rapidly. |
| 8 | groupEvents.ts | Bug | Uses `structuredClone(originalState)` which is a deep clone. But `pushFloat` and `logEvent` mutate the cloned state. However, the function returns the cloned state. This is a functional pattern, but if the caller doesn't reassign `state = sendRivalGift(state, ...)`, the mutations are lost. The codebase seems to use this pattern consistently, but it's error-prone. |
| 9 | groupEvents.ts | Bug | If `state.resources.gold >= 20`, it deducts gold and sets relationship to 'friendly'. But if the rival was already 'friendly', this is a no-op that costs gold. No check for existing relationship. |
| 10 | humanChat.ts | Bug | If `entityA.chatTicks > 0` or `entityB.chatTicks > 0`, returns early. But if entityB is null and entityA is already chatting, it still returns. However, `solo` chats should probably be allowed even if entityA is chatting? No, the check prevents interrupting existing chats. |
| 11 | IntroScreen.tsx | Bug | `return () => timers.forEach(clearTimeout);` - If component unmounts before timers fire, this cleans up. But if `phase` changes to 'ready' and component re-renders, the effect might not re-run because the dependency array is empty `[]`. Actually, the timeline effect has `[]` deps, so it only runs once. But `setPhase('ready')` is called inside a timer, which is fine. |
| 12 | lifeSimulation.ts | Logic Flaw | The function has a side effect of setting `entity.affairPartnerId` and `paramour.affairPartnerId` when progress reaches 100, but only if `entity.id < paramour.id`. If the affair is established, both get `affairPartnerId` set. But if one of them dies or is imprisoned later, `reconcileAffairPartner` is called in `tickHumans` to clean up. However, `reconcileAffairPartner` only checks `lover.affairPartnerId !== entity.id` or `lover.prisonBuildingId != null`. It doesn't check if `entity` is still alive. So if entity dies, the lover might keep the affairPartnerId until next tick. |
| 13 | lifeSimulation.ts | Logic Flaw | `if (entity.age >= HUMAN_MAX_LIFESPAN_YEARS \|\| Math.random() < oldAgeChance)` - If age >= max lifespan, death is guaranteed. But `oldAgeChance` is computed from `getOldAgeDeathChance(entity.age)`. If this returns 0 for ages below max, the random check is skipped. But if age is exactly max, it's guaranteed death. However, `HUMAN_MAX_LIFESPAN_YEARS` might be 80, and `getOldAgeDeathChance` might start increasing at 70. The logic seems okay but the sudden cliff at max lifespan is harsh. |
| 14 | lifeSimulation.ts | Bug | `if (offender.prisonBuildingId != null) { if (offender.prisonSentenceCrime === 'scandal') { offender.prisonerUntilTick = Math.max(offender.prisonerUntilTick ?? 0, newReleaseTick); } return; }` - If already imprisoned for a different crime, the function returns without extending sentence or changing prison. But the caller might expect the offender to be teleported to prison. |
| 15 | lifeSimulation.ts | Logic Flaw | `entity.energy -= energyLoss;` runs every tick. If a settler is at home, energyLoss is multiplied by 0.5. But if the settler is just standing near home (not arrived), they get full energy loss. The `hasWell` and `hasHospital` checks are done once per tick per human, which is O(n) with building scans. These could be cached in TickContext. |
| 16 | lifeSimulation.ts | Bug | `const mealHour = hourOfDay === 8 \|\| hourOfDay === 18;` - Meals only happen at exact hours 8 and 18. If a settler is off-screen and throttled, they might miss the meal hour because their tick only runs every N ticks. The `mealHour` check is exact, so if the throttled tick lands on hour 9, the settler misses breakfast. |
| 17 | lifeSimulation.ts | Bug | `nearRoad` check uses `roadAvoidance.isNearRoad(entity.x, entity.y)` if available. But `roadAvoidance` is built from `roadBuildings` in TickContext. If `roadBuildings` is empty, `roadAvoidance` is undefined. The fallback loop checks `Math.abs(r.x - entity.x) < r.width / 2 + 12` which is an AABB check, but roads can be rotated. The check ignores rotation, so a vertical road might not be detected correctly. |
| 18 | lifeSimulation.ts | Bug | `if (grassPopulation.alive < 500)` - Hardcoded cap of 500 grass. But on large maps, this might be too low. Also, `grassMult` is applied to reproduction chance but not to the cap. The cap is absolute, not density-based. |
| 19 | lifeSimulation.ts | Logic Flaw | `huntRange *= 1 + Math.min(3, nearbyPack) * 0.25;` - Pack bonus increases hunt range but the `findClosestInRadius` or loop still uses the original `huntRange` variable which is mutated. This is correct. But `nearbyPack` counts wolves within 120 units, which might include wolves on the other side of the map if the map is small. |
| 20 | lifeSimulation.ts | Bug | `if ((entity.type === EntityType.Wolf \|\| entity.type === EntityType.Fox \|\| entity.type === EntityType.Werewolf) && dist < 80 && isProductionTick(...))` - Tamed werewolves can hunt. But `isActiveMoonHowler` check is missing, so a cursed villager in werewolf form (who is tamed?) could hunt. Also, `preyFallback` is used as fallback if mobileGrid is not available, but it's `byType[EntityType.Rabbit].concat(byType[EntityType.Deer])` which might be undefined if those types don't exist in byType. |
| 21 | renderer.ts | Bug | `if (e.flash > 0 && !forEntityLayerCache)` - Flash overlay is skipped when drawing for entity layer cache. This means cached entities don't show flash effects. But the flash overlay is drawn separately in `drawEntityFlashOverlay`. However, animals with flash might not be in the cache if the cache is stale. |
| 22 | renderer.ts | Bug | `const frame = isWalking ? getHumanSpriteFrame(...) : getSpriteFrame(HUMAN_BASE_SPRITES[gender]);` - If `getHumanSpriteFrame` returns null (because `!ready`), it falls back to `getSpriteFrame`. But `getSpriteFrame` might also return null, causing `drawPioneerAt` to be called. However, `drawPioneerAt` expects a canvas context and might fail if the sprite frame is null. |
| 23 | saveLoad.ts | Bug | `world.buildings = (world.buildings \|\| []).map((b) => b.type === BuildingType.Workshop && !b.workshopRecipeId ? {...b, workshopRecipeId: DEFAULT_WORKSHOP_RECIPE_ID} : b)` - This only fixes missing workshopRecipeId, but doesn't handle other building migration cases. Also, `totalBuildingsCompleted` is recalculated but might not match the actual completed count if buildings were demolished. |
| 24 | saveLoad.ts | Logic Flaw | `applySaveMigration` is called with hardcoded strings. If multiple migrations apply, they all log events. But `world.appliedSaveMigrations` is initialized only if undefined. If the save already has migrations, new ones are appended. However, the check `if (world.appliedSaveMigrations.includes(id)) return;` prevents duplicates. |
| 25 | spatialGrid.ts | Bug | `const cellRadius = Math.ceil(radius / this.cellSize);` - This checks a square of cells, not a circle. Entities in corner cells of the square might be beyond radius but still checked. The callback receives `distSq` and must filter, which it does. But `recordSpatialCells` records the square cell count, not the actual circular area. |
| 26 | spatialGrid.ts | Bug | `isNearRoad` checks `Math.abs(road.x - x) < road.width / 2 + margin` - This is an AABB check that ignores road rotation. A diagonal road (if supported) would not be detected correctly. Also, roads are only horizontal or vertical based on rotation, so this is mostly correct. But `road.x` and `road.y` are centers, so the check should be `Math.abs(road.x - x) < road.width / 2 + margin && Math.abs(road.y - y) < road.height / 2 + margin`. This is correct for axis-aligned roads. |
| 27 | tickQueries.ts | Bug | If `mobileGrid` exists but `findClosestInRadius` returns null, the function returns undefined without checking `fallback`. This is correct behavior (grid is authoritative). But if `mobileGrid` is undefined and `fallback` is provided, it does a linear scan. The fallback might not be filtered by type, causing unnecessary predicate checks. |
| 28 | tradeCaravans.ts | Bug | `removeCarrier(state, route, entity)` sets `entity.alive = false` but doesn't remove the entity from `state.entities`. The entity remains in the array as a dead entity. This could cause the entity to still be iterated in some loops until cleanup. Also, `route.caravanCarrierId` is set to undefined, but if the entity is referenced elsewhere (e.g., selected entity), it becomes a dangling reference. |
| 29 | tradeCaravans.ts | Logic Flaw | `if (current + r.amount > max) return false;` - If ANY resource exceeds storage, the entire caravan is blocked. But the route might receive 0 of that resource. The check should skip resources with amount <= 0. Actually, it does `if (r.amount <= 0) continue;`, so this is correct. But if one resource is full and others are not, the caravan waits. This might be intentional. |
| 30 | victory.ts | Bug | `const wildWolves = state.entities.filter((e) => e.alive && e.type === EntityType.Wolf && e.tamedBy == null).length;` - `tamedBy` is not in the Entity type definition shown in the files. If it's an optional property, this is fine. But if it's missing from some wolf entities, all wolves might be counted as untamed. |
| 31 | worldEvents.ts | Bug | Fire kills entities within radius but doesn't check if they were already killed by another disaster this tick. Also, `killEntityInDisaster` calls `killHuman` for humans, which handles building occupancy cleanup. But for animals, it just sets `alive = false`. The animal's `huntTargetId` might still point to it. |
| 32 | nameLoader.ts | Bug | `const here = dirname(fileURLToPath(import.meta.url));` - `import.meta.url` might not be available in all environments (e.g., some bundlers). This could throw and fall back to embedded names. The catch block returns false, which triggers the fallback. This is correct. |
| 33 | moonHowler.ts | Bug | `were.type = EntityType.Human;` reverts type. But if the werewolf was killed while in wolf form, `revertToHumanForm` is never called. The entity stays as `EntityType.Werewolf` in death, which might affect save/load and statistics. |
| 34 | rendererLoader.ts | Bug | `if (rendererModule) { rendererModule.renderGame(...args); return; }` - If `rendererModule` is null, it queues the render call via `void rendererReady.then(...)`. But this returns a Promise that resolves asynchronously. The render call might happen after the frame is supposed to be drawn, causing a missed frame or flicker. |
| 35 | saveSchema.ts | Logic Flaw | `skills` is not in the save keys. Entity skills are lost on save/load. Also, `moonHowlerSaved` is not in the keys, so cursed villagers might lose their saved human state on load. |
| 36 | villageLeadership.ts | Logic Flaw | `experiencePoints = Math.round(Math.min(Math.max(0, entity.age - 16), 200) * 0.25);` - A 16-year-old gets 0 experience. A 216-year-old gets 50. But `entity.age` for humans is in years (via `getAgeInYears`), so this is reasonable. However, juveniles are filtered out by `isEligibleForLeadership`, so age >= 18 typically. |
| 37 | villageLeadership.ts | Bug | `if (dayInYear !== 0 \|\| state.pendingElectionYear == null \|\| year < state.pendingElectionYear \|\| state.electionCeremony) return false;` - If `year === state.pendingElectionYear` and `dayInYear === 0`, the ceremony starts. But if the leader dies on day 1, the vacancy election is scheduled for `year + 2`, not the current year. This is correct. But if the pending year arrives and there's no eligible candidate, `startElectionCeremony` returns false, and `pendingElectionYear` is set to null. The village remains without a leader. |
| 38 | terrainGen.ts | Bug | `if (typeof widthOrSize === 'string')` - The function signature accepts `number \| MapSize` for width and `number \| MapPreset` for height. But `MapPreset` is a string enum, so `typeof heightOrPreset === 'string'` checks for preset. However, if someone passes a number for width and a string for height, it works. But if they pass a string for width (MapSize) and a number for height, the height is treated as preset if it's a string, but numbers are treated as height. The logic is convoluted and error-prone. |
| 39 | renderer.ts | Bug | `const residentCountByBuilding = buildResidentCountByBuilding(_cachedHumans);` - This is called every frame. If `_cachedHumans` is empty (e.g., all humans off-screen), no glow is calculated. But buildings might still have occupants that are off-screen. The glow should use `residenceOccupants` from the sim, not just visible humans. |
| 40 | renderer.ts | Bug | `syncDrawCacheTick` resets `_cachedGrassKey` when tick changes. But if the camera moves without the tick changing, grass cache is not rebuilt. However, `syncGrassDrawCache` checks `grassKey` which includes camera position, so it does rebuild on camera move. The entity caches (trees, animals, humans) only rebuild on tick change, not on camera move. This means if the camera pans, newly visible entities won't appear until the next tick. |
| 41 | lifeSimulation.ts | Logic Flaw | `if (cheater.id >= paramour.id) return;` - Only the lower-ID partner triggers exposure. This is to prevent double-processing, but if the lower-ID partner is off-screen and throttled, the exposure might not happen even if the higher-ID partner is active. |
| 42 | research.ts | Bug | `state.researchProgress += speedMult;` - If `speedMult` is very high (e.g., from education multiplier), research can complete in less than one tick. But the check `if (state.researchProgress >= 100)` handles this. However, if multiple research speed bonuses stack multiplicatively, progress might jump from 0 to >100 in one tick, skipping intermediate values. |
| 43 | resourceUtils.ts | Bug | `const headroom = Number.isFinite(max) ? Math.max(0, max - current) : amount;` - If `max` is Infinity, it adds the full amount. But `state.storageMax` might have Infinity for gold. This is correct. But if `max` is 0 (shouldn't happen), headroom is 0. |
| 44 | renderer.ts | Bug | `for (const b of sorted)` sorts by `a.y + a.height / 2` for painter's algorithm. But if two buildings have the same Y, their order is arbitrary. This could cause incorrect overlap for buildings at the same depth. |
| 45 | placementUtils.ts | Bug | `if (b.faction === 'rival') continue;` - Rival buildings don't block placement. But visitor camps (which are not buildings in the `buildings` array) aren't checked. However, visitors are entities, not buildings. |
| 46 | groupEvents.ts | Bug | `const site = pickSite(state, center, 70, 160, avoid);` - If `avoid` includes many points and the map is crowded, `pickSite` might return a site very close to the center after 40 failed attempts. The fallback `return { x: Math.max(margin, Math.min(state.width - margin, anchor.x + minDist)), y: ... }` uses `anchor.x + minDist` which could exceed map bounds if not careful. Actually, `Math.min(state.width - margin, ...)` clamps it. |
| 47 | lifeSimulation.ts | Bug | `if (inElectionCeremony && state.electionCeremony)` - The ceremony target is computed via `getElectionGatherTarget(state, entity.id)`. But if the entity is not in the `attendees` list (e.g., a child or prisoner), `slot` becomes -1, and `ring` becomes `Math.floor(-1 / 12) = -1`, causing `ringRadius = 22 + (-1) * 14 = 8`. The entity gathers very close to the center. This might be intentional for non-eligible observers. |

## 🟡 Low Severity

| # | File | Type | Description |
|---|------|------|-------------|
| 1 | worldGen.ts | Logic Flaw | `needsWildlife` logic: `preyTotal < 22 && !(preyTotal >= 14 && predatorsOk)` is confusing. If preyTotal=15 and predatorsOk=true, needsWildlife=false (correct). If preyTotal=10 and predatorsOk=true, needsWildlife=true (correct). But if preyTotal=13 and predatorsOk=false, needsWildlife=true. The double negative is hard to reason about. |
| 2 | humanChat.ts | Logic Flaw | `void durationTicks; void entityId;` - These parameters are accepted but ignored in favor of dialogue tree system. The deprecated functions still exist but are misleading. |
| 3 | IntroScreen.tsx | Logic Flaw | `startedAtRef.current = Date.now();` is set in a useEffect with `[]`, but if the component remounts, it resets. However, the progress bar is just cosmetic. |
| 4 | lifeSimulation.ts | Logic Flaw | `if (idleRoll < 2)` uses `targetX = ((seed * 137.5) % (width * 0.6)) + width * 0.2`. If `seed * 137.5` is very large, the modulo might behave unexpectedly with floating point. Also, the seed formula `(entity.id * 7919 + Math.floor(tick / 1200)) % 1000` only gives 1000 possible destinations, causing clustering. |
| 5 | renderer.ts | Logic Flaw | `for (const hunter of hunters)` iterates over `_cachedAnimals` and `_cachedHumans`. But if `state.renderSoA` is true, it uses `getRenderSoABuckets().shims` to build `entityById`. However, `getRenderSoABuckets()` might return stale data if the cache hasn't been updated this frame. |
| 6 | populationGrowth.ts | Logic Flaw | `for (const entity of state.entities) { if (entity.alive && isPlayerHuman(entity)) pop += 1; }` - This counts all player humans, including children and imprisoned. The `getLivePlayerPopulation` function returns this count. But `PopulationPanel.tsx` uses `playerHumans.length` directly, which might differ if the panel's filter is different. |
| 7 | skills.ts | Logic Flaw | `skills[job] = Math.min(100, (skills[job] ?? 0) + amount);` - Skills can only increase, never decrease. There's no skill decay or cap beyond 100. Also, `amount` is typically 0.2, so reaching 100 takes 500 ticks of work, which might be too fast or too slow depending on tick rate. |
| 8 | terrainGen.ts | Logic Flaw | The spiral search starts at ring 1 and goes up to 40. If no buildable site is found, it returns the preferred position even if it's unbuildable. This could place the starting house in water or mountains. |
| 9 | lifeSimulation.ts | Logic Flaw | `entity.pregnantById = undefined;` - Married pregnancies have no `pregnantById`. But later in birth logic, `const biologicalFatherIdAtBirth = entity.pregnantById ?? entity.partnerId;` uses `partnerId` as fallback. This is correct for married couples. |
| 10 | preferences.ts | Logic Flaw | `if (raw === '0') return false; if (raw === '1') return true;` - Non-'0'/'1' values (including null) return true (default). This is fine but could be more explicit. |
| 11 | uiSimSummary.ts | Logic Flaw | `constructionWorkers` is rebuilt every call by iterating all buildings. This is O(buildings) and could be cached. Also, if `catalog` is provided, it uses `catalog.getPlayerHumans()` which might not include humans created this tick. |
| 12 | viewState.ts | Bug | `cam.targetX = worldX - (screenX - canvasW / 2) / newZoom;` - If `newZoom` is 0 (clamped away), this would divide by zero. But `clampCameraZoom` prevents zoom from reaching 0. |
| 13 | scentGrid.ts | Logic Flaw | `for (const entity of entities)` - Iterates all entities, not just mobile ones. If called with a large entity list, this is O(n). But it's typically called with predators only. |
| 14 | StatisticsPanel.tsx | Bug | `const max = Math.max(...data, 1);` - If `data` is very large, this spreads all elements as arguments, which could exceed stack size in extreme cases (though unlikely with yearly stats). |
| 15 | militiaBalance.ts | Logic Flaw | `const guardCount = getBarracksGuardCount(state, state.buildings);` - `getBarracksGuardCount` might count guards that are currently imprisoned or dead, if the building occupants list is stale. |
| 16 | renderer.ts | Logic Flaw | `strokeGridLines` is called with `whiteOutline > 0` but `whiteOutline` is always 0 in the calls. Dead parameter. |
| 17 | humanSprites.ts | Bug | `px(ctx, cx - 11, footY - 48 + bob, 22, 48, palette.outline);` - The silhouette is drawn first, then body parts. But if `bob` is non-zero, the silhouette and body might misalign slightly because `bob` is only applied to the silhouette Y, not to all body parts uniformly. Actually, `footY` already includes `bob`, so all parts use `footY` consistently. |