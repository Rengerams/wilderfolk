# v0.6.2.2 Objective Report — T1/T2 Baseline Evidence

- **Status:** verified
- **Date:** 2026-08-21
- **Objective:** Produce the renderer/game circular-chunk import trace and the dependency-cycle ledger before making any chunk-policy or simulation-import change.
- **Evidence:** `docs/V0_6_2_2_ROADMAP.md`, baseline production build, dependency-cruiser JSON snapshot, and the generated ledger.
- **Owner/cadence:** Vite chunk policy and renderer loader; build-time investigation. Dependency graph ownership is module-boundary analysis; no production simulation cadence is changed.
- **Scope:** Measurement and documentation only. No `manualChunks` rule, simulation import, worker path, save field, or gameplay rule was changed.
- **Non-goals:** No silent chunk-warning suppression, renderer rewrite, speculative boundary extraction, cycle-breaking refactor, Knip cleanup, version bump, release, commit, push, or publish action.

## Findings

The baseline production build completed successfully but reproduced the warning `Circular chunk: game-render -> game -> game-render`. Under the current chunk policy, the resolved source graph contains 88 `game-render → game` edges and one `game → game-render` edge. The reverse edge is the direct chunk-topology signal that must be reviewed before any boundary change; the renderer-to-game edges are mostly expected presentation dependencies on shared game values and helpers and must not be bulk-moved without an explicit render contract.

The complete dependency snapshot resolves three strongly connected components. This does not equal the historical audit’s 43 dependency-cycle warnings. The focused `npm run audit:deps:cycles` command currently reports zero modules and zero violations, so the result cannot be treated as proof that 43 cycles were removed. The discrepancy is recorded in the ledger for follow-up rather than hidden.

| Evidence | Result |
|---|---|
| Production build | Passed; circular chunk warning reproduced. |
| Resolved source graph | 3 strongly connected components; 88 renderer-to-game edges; 1 game-to-renderer edge. |
| Focused dependency-cycle audit | Reports 0 modules and 0 violations; requires command/config reconciliation. |
| Regression and type validation | Passed: 69 test files and 400 tests; TypeScript completed through `test:all`. |
| Repository lint | Not clean: 38 pre-existing errors, all reported under `skills/`; no changed Wilderfolk source was implicated. |
| Gameplay/save/worker impact | None; investigation only. |

## Simulation Change Record

- **Owner module:** Vite chunk policy and renderer loader for T1; module graph/dependency configuration for T2.
- **Decision changed:** None. The task records the existing import/chunk topology and cycle baseline.
- **Cadence:** Build-time.
- **State fields written:** None.
- **Why the change is needed:** Establish measured evidence before deciding whether a renderer/game boundary can be safely changed.
- **Player-visible behavior before:** Game builds and starts with the existing circular-chunk warning.
- **Player-visible behavior after:** No gameplay behavior changed; the warning and structural baseline are now documented.
- **Performance impact:** The baseline build completed in 8.73 seconds in the captured run. Worker-ready latency, first render, and normal frame behavior were not changed or benchmarked because no implementation change was made.
- **New or updated tests:** No gameplay tests required for a measurement-only task. Validation includes the production build, dependency snapshot generation, and reproducible ledger script.
- **Invariants checked:** No simulation mutation path, worker authority, cadence, save contract, or gameplay invariant was modified.
- **Save/migration impact:** None.
- **Rollback plan:** Remove the generated evidence report and analysis script; no runtime rollback is required.

## Verification and remaining risks

The evidence report is generated by `scripts/roadmap-t1-cycle-ledger.mjs` from `docs/_roadmap_t1_dependency_snapshot.json`. The build, focused dependency-audit, regression/type, and lint outputs are retained as local evidence files. The next action should reconcile the historical 43-warning count with the current dependency-cruiser invocation and review the one reverse chunk edge before proposing a minimal boundary change. The full test suite is green; repository lint remains blocked by 38 existing errors in `skills/`.

The detailed ledger is in `docs/V0_6_2_2_T1_IMPORT_TRACE_AND_CYCLE_LEDGER.md`. This objective is verified as an evidence-gathering task, not as a claim that T1’s circular chunk has been resolved.
